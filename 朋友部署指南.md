# 朋友部署指南 —— 3 步上线自己的汉化发布站

> 全程**不用装任何软件**，只要一个 GitHub 账号 + 一个浏览器。
> 做完这三步，你就有一个和自己站一模一样的发布站，链接是
> `https://你的用户名.github.io/仓库名/`

---

## 第一步：把模板复制到你自己的账号下

1. 没有 GitHub 账号先注册（有就跳过）：https://github.com/signup
2. 打开模板仓库：
   **https://github.com/cc-sketch413/mangguo**
3. 点右上角绿色的 **Use this template** → 再点 **Create a new repository**
4. **Repository name** 随便起个英文名，比如 `my-site`（这会出现在你的网址里）
5. 点绿色按钮 **Create repository**

> 复制的是一份**完全独立的副本**，以后你改自己的，不会影响原站。
>
> ⚠️ 如果打开链接后**看不到 "Use this template" 按钮**，说明站主还没把这个仓库
> 设成模板。跟她说一句「帮我把仓库设成模板仓库」即可；或者直接用**方式二**（见《从这里开始.md》），
> 效果完全一样。

---

## 第二步：开启网站（只需做一次）

1. 在你的新仓库里点 **Settings**（设置，在仓库顶部的标签栏里）
2. 左侧菜单往下找，点 **Pages**
3. 找到 **Build and deployment → Source** 下拉框，选 **GitHub Actions**
4. 不用管其他选项，页面会自动保存

> 直达链接：`https://github.com/你的用户名/仓库名/settings/pages`
>
> ⚠️ 这一步**没法自动化**（GitHub 不允许程序自己开启网站），所以必须手动点这一次。
> 忘了点的话，部署会失败并提示你回来点。

---

## 第三步：填上你自己的信息

1. 回到仓库首页，点开文件 **`我的设置.yml`**
2. 点右上角的 ✏️ 铅笔图标（Edit this file）
3. 改成你自己的内容：

| 设置项 | 说明 | 例子 |
|---|---|---|
| `站名` | 浏览器标签、首页顶部显示的名字 | `小明的汉化站` |
| `署名` | 页脚版权处的名字 | `小明` |
| `密码` | 粉丝解压文件、下载小说要输的密码 | `xiaoming` |
| `微博主页` | 你的微博链接，**没有就写 `""`** | `"https://weibo.com/u/123"` |
| `邮箱` | 粉丝联系你的邮箱，**没有就写 `""`** | `"me@qq.com"` |
| `主题色` | 网站主色调 | `"#f59e0b"`（暖橙） |

4. 拉到页面底部，点绿色按钮 **Commit changes**

**等 1~2 分钟**，然后访问：

```
https://你的用户名.github.io/仓库名/
```

看到自己的站就成功了 🎉

> 之后想改站名、换密码，**还是只改这一个文件**，改完等 1~2 分钟自动更新。
> 密码换了，压缩包解压密码、小说下载密码会**一起同步**，不用逐个去改。

---

## 接下来：往里放作品

站点刚复制时带着原站的示例作品，建议按顺序清理和添加：

1. **删掉示例作品**：把 `source/_posts/` 里的 `.md` 文件删掉（保留 `notice-start.md` 这份公告也行，改成你自己的话）
2. **加漫画作品**：照着 `source/_posts/` 里任一文件的格式，新建一个 `.md`
   （封面图片放 `source/img/cover/`）
3. **加小说下载**：把 TXT/EPUB 放进 `source/files/`，再在 `source/_data/novels.yml` 里加一条

详细写法见仓库里的 **`如何发布作品.md`** 和 **`使用说明.md`**。

---

## 常见问题

**Q：部署失败了，提示 "GitHub Pages 还没启用"？**
A：回到第二步，去 `Settings → Pages` 把 Source 选成 GitHub Actions。
选完回到 **Actions** 标签页，点最近一次的失败记录 → 右上角 **Re-run jobs**。

**Q：网站打开是 404？**
A：三种常见原因：
1. 刚提交完，还在构建中 —— 再等 1~2 分钟刷新
2. 没在 `Settings → Pages` 选 GitHub Actions（见上面）
3. 网址记错了 —— 必须是 `https://用户名.github.io/仓库名/`，仓库名大小写要一致

**Q：改完 `我的设置.yml` 网站没变化？**
A：去仓库的 **Actions** 标签页看看最近一次运行是不是成功了。如果失败了，
点进去看是不是 Pages 没开（Q1）。都正常的话再等一两分钟。

**Q：怎么知道我改的对不对？**
A：`我的设置.yml` 里每一行格式是「键: 值」，冒号后面**要有一个空格**。
改了名字之类的文字内容，用英文引号 `"` 包起来最保险。

**Q：想要自己的域名（比如 `xiaoming.com`）？**
A：先正常部署好，然后在 `Settings → Pages → Custom domain` 里填你的域名，
再去你的域名商那里按提示加一条解析。也可以直接用默认的 `github.io` 网址，完全够用。

**Q：会不会收费？**
A：不会。GitHub Pages 对公开仓库免费，流量额度（100 GB/月）个人站远远用不完。

---

## 一句话总结

| 步骤 | 做什么 | 要多久 |
|---|---|---|
| 1 | Use this template 复制仓库 | 1 分钟 |
| 2 | Settings → Pages 选 GitHub Actions | 30 秒（只做一次） |
| 3 | 改 `我的设置.yml` 并提交 | 2 分钟 |

之后每次更新，都只需要改 `我的设置.yml`（换信息）或加作品文件 —— 网站自动重新发布。
