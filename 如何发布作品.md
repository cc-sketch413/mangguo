# 如何发布一部作品（完整操作手册）

> 这份手册教你：**从一部汉化作品做完，到它出现在网站上让粉丝看到**，全程需要做什么。
> 照着一步步做就行，不需要懂编程。

---

## 一、先看全局：发布一部作品要经过 6 步

```
① 准备素材  →  ② 新建作品文件  →  ③ 填写内容
                                      ↓
⑥ 检查线上  ←  ⑤ 一键发布  ←  ④ 本地预览
```

| 步骤 | 你要做什么 | 大概耗时 |
|---|---|---|
| ① 准备素材 | 把作品信息、封面、网盘链接整理好 | 2 分钟 |
| ② 新建文件 | 在 `source\_posts\` 里建一个 `.md` 文件 | 30 秒 |
| ③ 填内容 | 照模板把信息和链接填进去 | 3~5 分钟 |
| ④ 本地预览 | 双击 `启动预览.bat` 看一眼 | 1 分钟 |
| ⑤ 一键发布 | 双击 `一键发布.bat` | 1 分钟 |
| ⑥ 检查线上 | 等 1~2 分钟刷新线上站点 | 2 分钟 |

---

## 二、两个文件夹，记住就行

站点根目录（下面都简称「站点目录」）：

```
C:\Users\D.Q\WorkBuddy\2026-09-16-16-31-17\汉化发布站
```

你平时只会碰这两个地方：

| 文件夹 | 放什么 | 位置 |
|---|---|---|
| **`source\_posts\`** | **作品文件**（每部作品一个 `.md` 文件） | 站点目录 `\source\_posts\` |
| **`source\img\cover\`** | **作品封面图** | 站点目录 `\source\img\cover\` |

---

## 三、第 1 步：准备素材

发布前先把这些东西准备好，免得填一半去翻：

- [ ] **作品中文名**（例：星屑烘焙屋）
- [ ] **原文名**（例：星屑ベーカリー，填日文原名）
- [ ] **类型**：漫画 / 小说
- [ ] **状态**：连载中 / 已完结 / 暂停 / 预告
- [ ] **进度**（例：已发布 3 / 12 话）
- [ ] **一句话简介**（显示在作品卡片上，20~40 字最好）
- [ ] **题材标签** 3~4 个（例：日常、治愈、美食）
- [ ] **封面图**（竖版，宽:高约 3:4，见第六步）
- [ ] **网盘链接 + 提取码**（总链接、最新话链接，有几个写几个）

---

## 四、第 2 步：新建作品文件

1. 打开站点目录，进入 **`source\_posts\`** 文件夹
2. **复制一个现成的文件**当模板（比如 `notice-start.md`），粘贴在同一个文件夹里
3. 把新文件**改名**：

```
文件名规则：用英文或拼音，不要中文、不要空格
✅ 好例子：hoshizora-bakery.md 、xingxie.md 、works-001.md
❌ 坏例子：星屑烘焙屋.md 、新作品 001.md 、作品(1).md
```

> ⚠️ **文件名不能以 `_`（下划线）开头**，否则网站会直接忽略它，等于没发。

---

## 五、第 3 步：填写内容（照抄这个模板）

用记事本或 VS Code 打开刚建的文件，**把下面这一整段复制进去，然后改内容**：

````markdown
---
title: 星屑烘焙屋
date: 2026-09-17 12:00:00
categories: [漫画]
tags: [日常, 治愈, 美食]
cover: /img/cover/hoshizora.jpg
description: 小镇面包店里的日常故事，温暖治愈系。
work_status: ongoing
work_original: 星屑ベーカリー
work_episodes: 已发布 3 / 12 话
---

{% workhead 漫画 || 连载中 || 作者名 || 已发布 3 / 12 话 %}

## 作品简介

在这里写两三句简介，让粉丝知道这是什么作品、讲什么故事。

## 汉化说明

说明嵌字字体、图片规格、有没有保留原文拟声词之类的细节。

{% tip danger %}
本站作品免费发布，请勿用于任何商业用途或二次售卖。发现倒卖请到[倒卖曝光墙](/expose/)举报。
{% endtip %}

## 下载

{% dlbox 漫画总链接 %}
{% dl 百度网盘 || https://pan.baidu.com/s/你的链接 || 提取码：abcd %}
{% enddlbox %}

{% dlbox 最新话（第 3 话） %}
{% dl 百度网盘 || https://pan.baidu.com/s/最新话链接 || 提取码：abcd %}
{% enddlbox %}

## 更新记录

- 2026-09-17 发布第 3 话
- 2026-09-10 发布第 2 话
- 2026-09-03 发布第 1 话
````

### 顶部那段（`---` 之间）每个字段什么意思

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | ✅ | 作品中文名 |
| `date` | ✅ | 首次创作/发布时间，格式 `年-月-日 时:分:秒` |
| `categories` | ✅ | **只能填 `[漫画]` 或 `[小说]`**，填错就不进作品库 |
| `tags` | 建议 | 题材标签，3~4 个 |
| `cover` | 建议 | 封面图路径，见第六步；留空会自动生成渐变封面（不难看，但不如真封面） |
| `description` | 建议 | 一句话简介，显示在卡片和搜索结果里 |
| `work_status` | 建议 | `ongoing` 连载中 / `completed` 已完结 / `hiatus` 暂停 / `planned` 预告 |
| `work_original` | 可选 | 原文名 |
| `work_episodes` | 可选 | 进度文案，例：`已发布 3 / 12 话` |

> ❌ **千万不要写 `updated` 字段**！网站靠「文件修改时间」自动判断谁是最新更新的。写了反而会出问题（详见第八步）。

### 正文里那些 `{% %}` 是什么

那是站点的「快捷标签」，帮你省掉写 HTML 的麻烦：

| 标签 | 用途 | 示例 |
|---|---|---|
| `{% workhead 类型 \|\| 状态 \|\| 原作者 \|\| 进度 %}` | 作品页顶部信息条 | `{% workhead 漫画 \|\| 连载中 \|\| 山田太郎 \|\| 3/12 话 %}` |
| `{% dlbox 标题 %} … {% enddlbox %}` | 下载按钮分组（带标题） | 常用「总链接」和「最新话」两个框 |
| `{% dl 名称 \|\| 链接 \|\| 备注 %}` | 单个下载按钮 | 链接会自动加密防扒，源码里看不到明文 |
| `{% tip info %} 内容 {% endtip %}` | 提示框 | 类型：`info` / `warn` / `ok` / `danger` |
| `{% progress 3 \|\| 12 %}` | 进度条 | 已发布 / 总数 |

> 💡 **重要**：多个参数之间用 **两根竖线 `||`** 分隔（不是一根！），前后可以带空格。

---

## 六、第 4 步：放封面图

1. 把封面图放进 **`source\img\cover\`** 文件夹
2. 文件名用英文/拼音，例如 `hoshizora.jpg`
3. 在作品文件的 `cover:` 后面写：`/img/cover/hoshizora.jpg`

**封面要求：**

| 项目 | 建议 |
|---|---|
| 比例 | **竖版 3:4**（和卡片形状一致，不会被裁变形） |
| 尺寸 | 宽 600~900 像素就够（太大会拖慢网站） |
| 格式 | `.jpg` 或 `.png` 都行 |
| 内容 | 建议用汉化作品的封面，或自己做的宣传图 |

> 偷懒办法：封面留空不填，网站会**自动按作品名生成一张彩色渐变封面**，不会开天窗。但真封面明显更好看。

---

## 七、第 5 步：本地预览（看看有没有写错）

双击站点目录里的 **`启动预览.bat`**：

- 浏览器会自动打开 `http://localhost:4000`
- 检查这几处：作品是否出现在**首页**和**作品库**、「下载」按钮点了能不能展开链接、封面显示是否正常
- 看完**关掉那个黑色窗口**就停止预览了

> ⚠️ **不要直接双击 `public\index.html` 看**！那样样式和图片全是坏的（会看到一坨裸文字）。只能通过 `启动预览.bat` 或线上地址看。

---

## 八、第 6 步：一键发布上线

确认没问题后，双击站点目录里的 **`一键发布.bat`**。

脚本会自动做三件事，你只需要等它跑完：

```
[1/3] 构建站点      → 把内容生成成网页文件
[2/3] 记录改动      → 打包本次修改
[3/3] 推送到线上    → 传送到 Netlify
```

看到 **「发布成功！」** 就完了。

**然后等 1~2 分钟**，Netlify 会自动重新部署。打开下面这个网址（按 Ctrl+F5 强制刷新）：

```
https://mangguo413.netlify.app
```

> 如果打开还是旧内容：等一会儿再刷新，或者按 `Ctrl + F5` 清缓存刷新。

### 如果脚本报错了

把黑色窗口里的**报错内容截图**发给我，我帮你看。常见原因是文件名带了 `_` 开头、`categories` 写错、或者链接格式写坏了。

---

## 九、日常维护：更新已有作品（会**自动置顶**）

作品更新新一话时，只要**编辑并保存那个作品文件**：

1. 把新一话的网盘链接，替换到「**最新话**」那个 `dlbox` 里
2. 在「**更新记录**」里加一行（新的写在最上面）
3. 如果进度变了，把 `work_episodes` 和 `workhead` 里的进度也改一下
4. **保存文件** → 双击 `一键发布.bat`

就这么简单。**不需要手动改任何时间**——网站会自动把这个作品顶到首页和作品库的最前面（因为它是「最近修改」的）。

---

## 十、日常维护：网盘链接失效补档

粉丝反馈链接失效时，做两件事：

1. **改作品文件**：把新的网盘链接替换进对应的 `{% dl %}` 里 → 保存
2. **加进补链合集**：打开 `source\relink\index.md`，照里面的注释模板加一条记录（作品名、失效时间、补链时间、新链接）

然后双击 `一键发布.bat`。粉丝打开「作品库 → 补链合集」就能看到新链接。

---

## 十一、常见问题

**Q：发了作品，网站上看不到？**
- 检查 `categories` 是不是写成了 `[漫画]` 或 `[小说]`（写成 `[漫画, 日常]` 或 `[Manga]` 都不进作品库）
- 检查文件名是不是以 `_` 开头（会被忽略）
- 检查有没有双击 `一键发布.bat`

**Q：下载按钮点了没反应？**
链接必须在 `{% dl 名称 || 链接 || 备注 %}` 里、用 `||` 分隔。检查是不是用了单根竖线或没写完整两组 `||`。

**Q：封面不显示？**
- 确认图片真的放进了 `source\img\cover\`
- `cover:` 后面的路径要以 `/img/cover/` 开头，例：`/img/cover/hoshizora.jpg`
- 文件名大小写要和实际文件一致（`Hoshizora.jpg` 和 `hoshizora.jpg` 是两回事）

**Q：发布脚本窗口一闪就没了？**
说明脚本位置不对。`一键发布.bat` 必须待在**站点根目录**（和 `_config.yml`、`_config.butterfly.yml` 同一层）。

**Q：能不能不自己操作，让你帮我发？**
当然可以。把作品信息（名字、类型、状态、链接、封面图）发我，我帮你建好文件、放好封面、发布上线，你只管验收。

---

## 十二、附录：不用脚本的手动命令行方式

如果你想自己敲命令（在站点目录打开 Git Bash 或终端）：

```bash
cd "C:\Users\D.Q\WorkBuddy\2026-09-16-16-31-17\汉化发布站"

hexo clean          # 清空旧产物
hexo generate       # 生成站点 → public/
hexo server         # 本地预览（Ctrl+C 停止）

# 发布上线
git add -A
git commit -m "发布新作品：作品名"
git push origin master
```

> `一键发布.bat` 就是把「generate + add + commit + push」串起来自动跑，效果完全一样。

---

## 附：这套发布流程背后的原理（了解即可）

```
你的电脑（站点目录）
   │  ① 双击 一键发布.bat
   ↓
GitHub 仓库（cc-sketch413/mangguo）   ← 代码的中转仓库
   │  ② Netlify 检测到仓库有更新，自动拉取
   ↓
Netlify 服务器
   │  ③ 自动执行 npm run build（等效于 hexo generate）
   ↓
线上网站  https://mangguo413.netlify.app
```

所以你**只要管好自己电脑上的文件 + 双击脚本**，剩下的 GitHub 和 Netlify 都是全自动的。
