# 汉化发布站 · 使用说明

这是一个给**粉丝**用的汉化作品发布站，基于 **Hexo + Butterfly** 主题，和参考站「漂流的终点」同一套框架。纯静态，免费托管，没有后端和数据库。

- **粉丝视角**：进来 → 首页/作品库看作品 → 点进作品页 → 拉到「下载」区点网盘链接
- **你（作者）视角**：改一个数据文件加作品，跑一条命令出站，剩下的不用管

---

## 一、站点基本信息（已完成）

打开站点根目录的 **`_config.yml`**，这三个地方已经填好，以后想改就改这里：

| 键 | 当前值 | 说明 |
|---|---|---|
| `title` | `芒果的发布站` | 全站标题、浏览器标签 |
| `author` | `一个芒果OVO` | 页脚、RSS 作者名 |
| `url` | `https://mangguo413.com` | RSS、站点地图、分享卡片 |

改完 `url` 之后**必须重新生成**（`hexo generate`），RSS 和站点地图里的链接才会更新。

> 站名副标题在 `_config.yml` 的 `subtitle`；如果想改导航栏上显示的站名样式，去 `_config.butterfly.yml`。

---

## 二、怎么加一部新作品

### 方式 A：复制模板（推荐）

1. 复制 `scaffolds/work.md` 为 `source/_posts/作品id.md`（作品 id 用英文/拼音，比如 `hoshizora.md`）
2. 填 front-matter（文件顶部 `---` 之间的部分）和正文
3. 重新生成

### 方式 B：用命令生成

```bash
hexo new work "作品id"
```

会按 `scaffolds/work.md` 生成，然后再补正文。

### front-matter 怎么写

```yaml
---
title: 星屑烘焙屋            # 作品中文名
date: 2026-06-18 12:00:00   # 首发时间
# ★ 不要写 updated 字段：站点按「文件最后修改时间」自动置顶，
#   更新作品时只要编辑并保存这个文件，首页和作品库就会自动把它排到最前面。
categories: [漫画]           # 漫画 或 小说（决定进哪个分类，务必填对）
tags: [日常, 治愈, 美食]     # 题材标签 3~4 个
cover:                      # 封面图，如 /img/cover/xxx.jpg；留空自动生成渐变封面
description: 一句话简介。    # 显示在卡片和搜索结果里
work_status: ongoing        # ongoing 连载中 / completed 已完结 / hiatus 暂停 / planned 预告
work_original: 星屑ベーカリー # 原文名
work_episodes: 已发布 2 / 12 话 # 进度文案
---
```

### 正文用什么标签（不用写 HTML）

| 标签 | 用途 | 示例 |
|---|---|---|
| `{% workhead 漫画 \|\| 连载中 \|\| 原作 \|\| 2/12 话 %}` | 作品页顶部信息条（类型/状态/原作者/进度） | 每个作品页开头放一个 |
| `{% dlbox 标题 %}…{% enddlbox %}` | 带标题的下载分组容器 | 标题可选，如「漫画总链接」「最新话」；包住下面的多个 `dl` |
| `{% dl 百度网盘 \|\| 链接 \|\| 提取码：xxx %}` | 单个下载按钮 | 三个参数：名称、链接、备注（链接已自动防扒，网页源码里看不到明文） |
| `{% tip warn %}…{% endtip %}` | 提示框 | 类型可选 `info` / `warn` / `ok` / `danger` |
| `{% progress 2 \|\| 12 %}` | 进度条 | 已发布 / 总数 |
| `{% worklist %}` 或 `{% worklist 漫画 %}` | 自动列出全部/指定分类作品 | 用在「作品库」「关于」页 |

参数之间用 **`||`** 分隔（不是竖线 `|`，是两根竖线），避免空格和引号问题。

一个完整作品页的结构：`workhead` → 作品简介 → 汉化说明 → `tip` 免责 → 下载（**「漫画总链接」和「最新话」两个 `dlbox`**）→ 更新记录。照抄 `scaffolds/work.md` 模板即可。

**更新作品 = 自动置顶**：把新一话链接换到「最新话」框里、在更新记录里加一行，保存文件即可——首页和作品库都按「最近更新」排序，这个作品会自己顶到最前，不用手动改任何时间字段。

---

## 三、发公告

公告也是文章，只是 `categories` 写 `[公告]`。示例见 `notice-start.md`。

公告会：
- 出现在首页
- 出现在「公告」分类里（导航「分类」→「公告」）
- 推送到 RSS

公告不会出现在「作品库」的卡片网格里（`worklist` 标签已自动排除公告）。

---

## 四、改外观

### 主题色

`_config.butterfly.yml` 里搜 `theme_color`，改 `main` 那一行。当前是芒果橙 `#f59e0b`，改一个值全站生效。

### 导航菜单

`_config.butterfly.yml` 的 `menu` 段，格式是 `菜单名: 路径 || 图标`。

### 自定义组件样式

`source/css/custom.css`，管下载按钮、提示框、进度条、作品卡片网格、作品信息条。改了重新 `hexo generate`。

---

## 五、评论系统（可选，让粉丝在「失效反馈」留言）

目前评论用的是 **Twikoo**，还没配置。要启用：

1. 去 Twikoo 官网（`twikoo.js.org`）申请一个免费的 envId（可用其免费云服务，不用自己买服务器）
2. 把 envId 填进 `_config.butterfly.yml` 的 `twikoo.envId`
3. 重新生成

不启用也不影响主流程，只是评论区不会出现。哪些页面要开评论，由文章 front-matter 的 `comments: true/false` 控制（`report`、`guide`、作品页已默认开）。

---

## 六、生成、预览、上线

> **⚠️ 重要：不要直接双击 `public/index.html`！**
> Hexo 生成的页面用的是 `/css/...` 这种绝对路径，双击打开（file://）会找不到样式和图片，
> 看到的就是一片裸文字加裂图。本地看站只有两种正确方式：
>
> 1. **双击站点目录里的 `启动预览.bat`**（推荐）—— 自动起服务并打开浏览器；
> 2. 或在终端手动跑 `hexo server`，然后访问 http://localhost:4000 。
>
> 想给粉丝看，就按下面的方法部署上线，线上环境不存在这个问题。

```bash
cd 汉化发布站

hexo clean       # 清空旧输出
hexo generate    # 生成静态站点 → public/ 目录
hexo server      # 本地预览（默认 http://localhost:4000）
```

### 上线（三选一，都是免费的）

| 平台 | 做法 | 适合 |
|---|---|---|
| **Netlify / Vercel** | 把 `public/` 文件夹拖进去，或连 Git 仓库自动构建 | 最省事 |
| **GitHub Pages** | 推 `public/` 到仓库，或配 `_config.yml` 的 `deploy` 段 | 想免费+自定义域名 |
| **任何静态托管** | 把 `public/` 里所有文件传上去即可 | 已有服务器/空间 |

上线后改东西，就是「改文件 → `hexo generate` → 把 `public/` 再传一遍」。

---

## 七、常见问题

**Q：加了新作品，站里没显示？**
八成是 `categories` 没写成 `[漫画]` 或 `[小说]`，或者忘了 `hexo generate`。

**Q：网盘链接失效了怎么补档？**
改对应作品页 `_posts/xxx.md` 里的 `{% dl %}` 链接 → 保存文件（自动置顶）→ 重新生成 → 在「公告」发一条补档说明。

**Q：搜索不到刚加的作品？**
`hexo clean && hexo generate` 再试。搜索数据在 `search.xml` 里，clean 后会重建。

**Q：图标不显示 / 页面加载慢？**
图标和部分脚本从 jsdelivr CDN 加载，国内偶尔慢。可在 `_config.butterfly.yml` 的 `CDN` 段把 `third_party_provider` 从 `jsdelivr` 换成 `unpkg` 再试。

---

## 附：本项目的关键点（备忘）

- 自定义标签都在 `scripts/tags.js`，**直接调用 `hexo` 全局变量**注册（不要用 `module.exports = function(hexo){}` 包裹，Hexo 8 的 scripts 沙箱会把它当成普通函数不执行）。
- 作品数据就是 `source/_posts/` 下的 markdown，无数据库。
- 每篇作品记得把下载按钮（`{% dl %}`）里的链接换成真实网盘地址，不要留占位。
