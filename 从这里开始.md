# 从这里开始 👋

这是一个完整的「个人汉化作品发布站」，开箱即用。功能包括：

- **作品墙**：封面网格、最近更新、完结合集
- **漫画下载**：网盘链接 + 提取码一键复制，链接做了防采集处理
- **小说下载**：输入密码后**直接下载 zip / epub**，不用转网盘（每本小说一个独立密码）
- **18+ 年龄确认门**：进站先确认成年，选「否」会全屏拦住
- **配套页面**：获取须知、失效反馈、倒卖曝光墙、补链合集

---

## 怎么把它变成你自己的网站

### 方式一：用模板一键复制（最省事，推荐）

如果对方给了你一个 GitHub 链接：

1. 打开那个链接，点绿色的 **Use this template** → **Create a new repository**
2. 仓库名随便起个英文名（比如 `my-site`，会出现在你的网址里），点 **Create repository**
3. 去 **Settings → Pages**，把 **Source** 选成 **GitHub Actions**（只需做一次）
4. 打开 **`我的设置.yml`** 改成你自己的信息，点 **Commit changes**

等 1~2 分钟，访问 `https://你的用户名.github.io/仓库名/`

> 详细图文步骤（含每一步的直达链接和常见问题）见 **《朋友部署指南.md》**。

### 方式二：用这个文件夹（没有模板链接时）

1. 注册 GitHub 账号：https://github.com/signup
2. 点右上角 **+** → **New repository**，填一个英文仓库名，**不要**勾选 Add a README
3. 在空仓库页面点 **uploading an existing file**，把本文件夹里的**所有文件**拖进去，提交
4. 去 **Settings → Pages**，把 **Source** 选成 **GitHub Actions**
5. 打开 **`我的设置.yml`** 改成你自己的信息，提交

---

## 最重要的一件事 ⭐

**所有个人设置都在这一个文件里：`我的设置.yml`**

| 设置项 | 说明 |
|---|---|
| `站名` | 浏览器标签、首页顶部显示的名字 |
| `署名` | 页脚版权处的名字 |
| `密码` | 漫画压缩包的解压密码 |
| `微博主页` / `邮箱` | 你的联系方式，没有就写 `""` |
| `主题色` | 网站主色调 |

改完提交，**1~2 分钟后全站自动更新**，不用碰其他任何文件。
（小说下载密码是每本小说单独设的，加小说时在 `novels.yml` 里写 `password: 你的密码`（明文即可），详见《文件夹放什么.txt》。）

---

## 记得清理这两处

这个包里带着原始站主的示例内容，用之前请清掉：

1. **删掉示例作品**：`source/_posts/` 里除你自己的作品外，全部删掉
2. **改掉说明文案**：`source/guide/index.md`（获取须知）、`source/about/index.md`（关于本站）、
   `source/report/index.md`（失效反馈）里如果有提到原站主的地方，改成你自己的

---

## 以后怎么加内容

| 想做什么 | 看哪个文档 |
|---|---|
| **搞不清东西该放哪** | **《文件夹放什么.txt》** ← 建议先看这份 |
| 加漫画作品、改下载链接 | **《如何发布作品.md》** |
| 改站名、换主题、日常维护 | **《使用说明.md》** |
| 加小说（文件 + 每本独立密码） | `source/_data/novels.yml` 里有注释说明，密码直接写明文，照着写一条即可 |

文件放哪里：
- 作品封面 → `source/img/cover/`
- 小说文件（zip / epub）→ `source/files/`

> 💡 **《文件夹放什么.txt》** 是纯文本文件，双击就能用记事本打开，
> 里面画了一张目录结构图，标清了「哪些文件夹要动、哪些千万别动」，以及「我要做 X 该去哪个文件夹」的对照表。

---

## 常见问题

**网站打不开 / 404？**
1. 刚提交完要多等 1~2 分钟
2. 确认 **Settings → Pages** 里 Source 选了 **GitHub Actions**
3. 网址必须是 `https://用户名.github.io/仓库名/`（仓库名大小写要一致）

**部署失败了？**
去仓库的 **Actions** 标签页点开最近一次运行，看看报错。最常见的就是 Pages 没开（见上一条）。

**要花钱吗？**
不要。GitHub Pages 对公开仓库免费，个人站的用量远远够用。

**能用自己的域名吗？**
可以。先在 `Settings → Pages → Custom domain` 里填域名，再去域名商那里加解析。
用默认的 `github.io` 网址也完全够用。
